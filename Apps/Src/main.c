// place holder for main.c
#include "FreeRTOS.h"


int main() {

    
    // start the scheduler
    while(1){
        // Scheduler should've started by now
        // Code should never enter this point
    }
    return 0;
}